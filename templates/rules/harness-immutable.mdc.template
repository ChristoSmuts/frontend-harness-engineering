---
description: Harness files are maintained by humans or harness-owner agents only
alwaysApply: true
globs: "AGENTS.md,**/ORCHESTRATION.md,**/.cursor/skills/**,**/.claude/skills/**,**/.agents/skills/**,HARNESS_CHANGELOG.md"
---

# Harness immutability (feature work)

During normal feature implementation, **do not edit** harness configuration unless the user explicitly asks to update agent guidance.

- Edit application code under `{{SCOPE_GLOBS}}` only.
- Harness changes belong in a dedicated PR reviewed by {{HARNESS_OWNER}}.
- If a repeatable agent failure requires a harness fix, propose a minimal diff and stop.
